# Armbian build directory (cloned at build time)
.armbian-build/
armbian/armbian-build

# Build outputs
debs/
output/

# OS artifacts
*.img
*.img.xz

# Pre-baked container tarballs staged into the image at build time
# (docker-save'd by the CI bundle step; never committed)
armbian/userpatches/extensions/airwaves-os/images/*.tar

# Dev environment
dev/config/

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~
