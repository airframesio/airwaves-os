display_alert "Source" "config-airwaves" "info"

# Source the common configuration
source "${SRC}/userpatches/common-airwaves.conf"

# Board/branch-specific overrides can be set here or via targets.yaml
# Example:
# [[ "${BOARD}" == "rpi4b" ]] && declare -g BOOTSIZE="256"
